query ($journalpostId: String!) {
    $journalpost(
        journalpostId: $journalpostId
    ) {
        journalpostId
        dokumenter {
            dokumentInfoId
            tittel
        }
        avsenderMottaker {
            id
            navn
            type
        }
    }
}